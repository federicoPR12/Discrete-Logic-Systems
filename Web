<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discrete Logic Systems | Automated Legal Intake</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,600&family=Syne:wght@400;500;600;700;800&family=Outfit:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <style>
        :root {
            --bg:          #060810;
            --surface:     #0d1221;
            --surface-2:   #141e32;
            --accent:      #00c8ff;
            --accent-glow: rgba(0, 200, 255, 0.12);
            --text:        #dde6f5;
            --text-muted:  #4f6580;
            --border:      #192438;
            --border-lite: #243050;
        }

        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; }

        body {
            background: var(--bg);
            color: var(--text);
            font-family: 'Outfit', sans-serif;
            font-weight: 400;
            line-height: 1.75;
            overflow-x: hidden;
        }

        /* ---- TYPE ---- */
        h1, h2, h3 {
            font-family: 'Cormorant Garamond', serif;
            font-weight: 600;
            letter-spacing: -0.02em;
        }
        .label {
            font-family: 'Syne', sans-serif;
            font-size: 0.68rem;
            letter-spacing: 0.18em;
            text-transform: uppercase;
            color: var(--accent);
            font-weight: 700;
        }

        /* ---- LAYOUT ---- */
        .container { max-width: 1100px; margin: 0 auto; padding: 0 2.5rem; }

        /* ---- NAV ---- */
        nav {
            position: fixed; top: 0; left: 0; right: 0; z-index: 100;
            display: flex; justify-content: space-between; align-items: center;
            padding: 1.4rem 2.5rem;
            background: rgba(6, 8, 16, 0.82);
            backdrop-filter: blur(16px);
            border-bottom: 1px solid var(--border);
        }
        .nav-logo {
            font-family: 'Syne', sans-serif; font-weight: 800;
            font-size: 1.05rem; color: #fff; text-decoration: none;
            letter-spacing: 0.02em;
        }
        .nav-logo span { color: var(--accent); }
        .nav-links { display: flex; gap: 2.25rem; list-style: none; }
        .nav-links a {
            color: var(--text-muted); text-decoration: none;
            font-family: 'Syne', sans-serif; font-size: 0.82rem;
            font-weight: 500; letter-spacing: 0.06em;
            transition: color 0.2s;
        }
        .nav-links a:hover { color: var(--accent); }
        .nav-cta {
            background: var(--accent) !important; color: #060810 !important;
            padding: 0.45rem 1.25rem; border-radius: 3px; font-weight: 700 !important;
        }
        .nav-cta:hover { background: #fff !important; }

        /* ---- HERO ---- */
        #hero {
            position: relative; min-height: 100vh;
            display: flex; align-items: center; overflow: hidden;
        }
        #hero-canvas {
            position: absolute; inset: 0; z-index: 0;
        }
        .hero-overlay {
            position: absolute; inset: 0; z-index: 0;
            background: radial-gradient(ellipse 70% 60% at 60% 50%, transparent 0%, var(--bg) 75%);
        }
        .hero-content {
            position: relative; z-index: 1;
            max-width: 800px; padding-top: 5rem;
        }
        .hero-eyebrow {
            display: inline-flex; align-items: center; gap: 0.6rem;
            margin-bottom: 2.25rem;
        }
        .hero-eyebrow::before {
            content: ''; display: block;
            width: 28px; height: 1px; background: var(--accent);
        }
        h1.hero-title {
            font-size: clamp(2.8rem, 5.5vw, 5.2rem);
            line-height: 1.08; color: #fff; margin-bottom: 1.6rem;
        }
        h1.hero-title em { font-style: italic; color: var(--accent); }
        .hero-sub {
            font-size: 1.1rem; color: var(--text-muted);
            max-width: 540px; margin-bottom: 2.75rem;
            font-weight: 300; line-height: 1.8;
        }
        .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; }
        .btn-primary {
            background: var(--accent); color: #060810;
            padding: 0.9rem 2.1rem; border-radius: 3px;
            font-family: 'Syne', sans-serif; font-weight: 700;
            font-size: 0.88rem; text-decoration: none;
            letter-spacing: 0.07em; transition: all 0.25s; display: inline-block;
        }
        .btn-primary:hover { background: #fff; transform: translateY(-2px); }
        .btn-ghost {
            border: 1px solid var(--border-lite); color: var(--text-muted);
            padding: 0.9rem 2.1rem; border-radius: 3px;
            font-family: 'Syne', sans-serif; font-weight: 600;
            font-size: 0.88rem; text-decoration: none;
            letter-spacing: 0.07em; transition: all 0.25s; display: inline-block;
        }
        .btn-ghost:hover { border-color: var(--accent); color: var(--accent); }
        .hero-scroll {
            position: absolute; bottom: 2.5rem; left: 50%;
            transform: translateX(-50%); z-index: 1;
        }
        .scroll-line {
            width: 1px; height: 55px; margin: 0 auto;
            background: linear-gradient(to bottom, var(--accent), transparent);
            animation: pulse 2.2s ease-in-out infinite;
        }
        @keyframes pulse { 0%,100%{opacity:.2} 50%{opacity:1} }

        /* ---- PROBLEM ---- */
        #problem { padding: 9rem 0; background: var(--surface); }
        .section-header { margin-bottom: 4rem; }
        .section-header h2 {
            font-size: clamp(2rem, 3.5vw, 3.2rem);
            margin-top: 0.8rem; margin-bottom: 1rem;
        }
        .section-header p { color: var(--text-muted); font-size: 1rem; max-width: 520px; }
        .problem-grid {
            display: grid; grid-template-columns: repeat(3,1fr);
            gap: 1.5px; background: var(--border);
        }
        .problem-card {
            background: var(--surface); padding: 2.75rem 2.25rem;
            transition: background 0.35s;
        }
        .problem-card:hover { background: var(--surface-2); }
        .problem-num {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3.8rem; font-weight: 700;
            color: var(--accent); opacity: 0.35;
            line-height: 1; margin-bottom: 1.25rem;
        }
        .problem-card h3 {
            font-size: 1.25rem; margin-bottom: 0.8rem; color: #fff;
        }
        .problem-card p { color: var(--text-muted); font-size: 0.92rem; line-height: 1.75; }

        /* ---- HOW IT WORKS ---- */
        #how { padding: 9rem 0; background: var(--bg); }
        .flow-steps {
            display: grid; grid-template-columns: repeat(3,1fr);
            gap: 1.75rem; margin-top: 4rem;
        }
        .step-card {
            background: var(--surface); border: 1px solid var(--border);
            border-radius: 8px; padding: 2.5rem 2rem;
            cursor: pointer; transition: border-color 0.3s, box-shadow 0.3s;
            transform-style: preserve-3d; will-change: transform;
        }
        .step-card:hover {
            border-color: var(--accent);
            box-shadow: 0 0 48px var(--accent-glow);
        }
        .step-idx {
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.7rem; color: var(--accent);
            opacity: 0.6; margin-bottom: 1.5rem;
        }
        .step-icon {
            width: 46px; height: 46px; border-radius: 8px;
            border: 1px solid var(--border-lite);
            display: flex; align-items: center; justify-content: center;
            font-size: 1.4rem; margin-bottom: 1.5rem;
        }
        .step-card h3 { font-size: 1.2rem; color: #fff; margin-bottom: 0.7rem; }
        .step-card > p { color: var(--text-muted); font-size: 0.9rem; line-height: 1.75; }
        .step-detail {
            margin-top: 1.5rem; padding-top: 1.5rem;
            border-top: 1px solid var(--border);
            display: none; animation: fadeIn 0.3s ease;
        }
        .step-detail.open { display: block; }
        @keyframes fadeIn { from{opacity:0;transform:translateY(6px)} to{opacity:1;transform:translateY(0)} }
        .step-detail code {
            font-family: 'JetBrains Mono', monospace; font-size: 0.73rem;
            color: var(--accent); background: var(--surface-2);
            padding: 1rem 1.25rem; border-radius: 4px; display: block;
            line-height: 2; border: 1px solid var(--border);
        }

        /* ---- DEMO ---- */
        #demo { padding: 9rem 0; background: var(--surface); }
        #demo .section-header { text-align: center; }
        #demo .section-header p { margin: 0 auto; }
        .demo-wrap {
            margin-top: 3.5rem; border-radius: 12px; overflow: hidden;
            border: 1px solid var(--border-lite); background: var(--surface-2);
            aspect-ratio: 16/9; display: flex;
            align-items: center; justify-content: center;
        }
        .demo-placeholder { text-align: center; }
        .demo-placeholder p {
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.82rem; color: var(--text-muted);
        }

        /* ---- PRICING ---- */
        #pricing { padding: 9rem 0; background: var(--bg); }
        #pricing .section-header { text-align: center; }
        #pricing .section-header p { margin: 0 auto; }
        .pricing-card {
            max-width: 700px; margin: 4rem auto 0;
            border: 1px solid var(--border-lite); border-radius: 12px;
            overflow: hidden;
        }
        .pricing-head {
            background: var(--surface-2); padding: 2.5rem;
            border-bottom: 1px solid var(--border);
        }
        .pricing-head h3 { font-size: 1.5rem; color: #fff; margin-bottom: 0.4rem; }
        .pricing-head p { color: var(--text-muted); font-size: 0.9rem; }
        .pricing-body { padding: 2.5rem; background: var(--surface); }
        .pricing-row {
            display: flex; justify-content: space-between;
            align-items: flex-start; padding: 1.4rem 0;
            border-bottom: 1px solid var(--border);
        }
        .pricing-row:last-of-type { border-bottom: none; }
        .p-label { font-weight: 500; color: var(--text); font-size: 0.95rem; }
        .p-desc { color: var(--text-muted); font-size: 0.84rem; margin-top: 0.2rem; }
        .p-amount {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem; font-weight: 700; color: var(--accent);
            white-space: nowrap; padding-left: 2rem; text-align: right;
        }
        .p-amount small {
            font-family: 'Outfit', sans-serif; font-size: 0.72rem;
            color: var(--text-muted); display: block; margin-top: 0.1rem;
        }
        .pricing-includes {
            margin-top: 1.75rem; padding-top: 1.75rem;
            border-top: 1px solid var(--border);
        }
        .pricing-includes .label { display: block; margin-bottom: 1.1rem; }
        .include-item {
            display: flex; align-items: center; gap: 0.8rem;
            padding: 0.45rem 0; font-size: 0.9rem; color: var(--text-muted);
        }
        .include-item::before {
            content: ''; width: 5px; height: 5px;
            border-radius: 50%; background: var(--accent); flex-shrink: 0;
        }

        /* ---- ABOUT ---- */
        #about { padding: 9rem 0; background: var(--surface); }
        .about-grid {
            display: grid; grid-template-columns: 1fr 1fr;
            gap: 5rem; margin-top: 4rem; align-items: center;
        }
        .about-text h2 { font-size: clamp(1.8rem,2.8vw,2.5rem); margin: 0.8rem 0 1.5rem; }
        .about-text p { color: var(--text-muted); font-size: 0.97rem; margin-bottom: 1.2rem; line-height: 1.8; }
        .stat-grid {
            display: grid; grid-template-columns: 1fr 1fr;
            gap: 1.5px; background: var(--border);
        }
        .stat-box {
            background: var(--surface); padding: 2.25rem;
            text-align: center; transition: background 0.3s;
        }
        .stat-box:hover { background: var(--surface-2); }
        .stat-num {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2.4rem; font-weight: 700; color: var(--accent);
            display: block; line-height: 1.1;
        }
        .stat-lbl {
            font-family: 'Syne', sans-serif; font-size: 0.7rem;
            letter-spacing: 0.1em; color: var(--text-muted); margin-top: 0.4rem;
        }

        /* ---- CTA ---- */
        #contact { padding: 9rem 0; background: var(--bg); text-align: center; }
        #contact h2 {
            font-size: clamp(2rem,4vw,3.8rem);
            max-width: 620px; margin: 1rem auto 1.5rem;
        }
        #contact > .container > p {
            color: var(--text-muted); max-width: 450px; margin: 0 auto 3rem;
        }

        /* ---- FOOTER ---- */
        footer {
            background: var(--surface); border-top: 1px solid var(--border);
            padding: 2rem; text-align: center;
        }
        footer p {
            color: var(--text-muted); font-family: 'Syne', sans-serif;
            font-size: 0.72rem; letter-spacing: 0.1em;
        }

        /* ---- SCROLL ANIMATIONS ---- */
        .fade-up {
            opacity: 0; transform: translateY(28px);
            transition: opacity 0.75s ease, transform 0.75s ease;
        }
        .fade-up.visible { opacity: 1; transform: translateY(0); }

        /* ---- RESPONSIVE ---- */
        @media (max-width: 800px) {
            .problem-grid, .flow-steps { grid-template-columns: 1fr; }
            .about-grid { grid-template-columns: 1fr; gap: 3rem; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>

<!-- NAV -->
<nav>
    <a href="#" class="nav-logo">Discrete <span>Logic</span> Systems</a>
    <ul class="nav-links">
        <li><a href="#how">How It Works</a></li>
        <li><a href="#demo">Demo</a></li>
        <li><a href="#pricing">Pricing</a></li>
        <li><a href="#contact" class="nav-cta">Get Started</a></li>
    </ul>
</nav>

<!-- HERO -->
<section id="hero">
    <canvas id="hero-canvas"></canvas>
    <div class="hero-overlay"></div>
    <div class="container">
        <div class="hero-content">
            <div class="hero-eyebrow label">Automated Intake Architecture</div>
            <h1 class="hero-title">Every missed call is a case<br><em>your competitor is closing.</em></h1>
            <p class="hero-sub">Discrete Logic Systems builds custom intake automation for law firms. When a call goes unanswered, the system responds instantly, triages the inquiry, and routes the lead before they move on.</p>
            <div class="hero-actions">
                <a href="#demo" class="btn-primary">Watch the Demo</a>
                <a href="#how" class="btn-ghost">See How It Works</a>
            </div>
        </div>
    </div>
    <div class="hero-scroll"><div class="scroll-line"></div></div>
</section>

<!-- PROBLEM -->
<section id="problem">
    <div class="container">
        <div class="section-header fade-up">
            <span class="label">The Problem</span>
            <h2>Your intake process has a gap</h2>
            <p>New case inquiries arrive at unpredictable hours. The firm that responds first is almost always the firm that converts.</p>
        </div>
        <div class="problem-grid">
            <div class="problem-card fade-up">
                <div class="problem-num">01</div>
                <h3>The window closes fast</h3>
                <p>A potential client who hits voicemail is already scrolling to the next result. There is no grace period for a callback the following morning.</p>
            </div>
            <div class="problem-card fade-up">
                <div class="problem-num">02</div>
                <h3>Voicemail is a dead end</h3>
                <p>Most inbound callers will not leave a message. Those who do rarely receive a callback within a competitive window. The lead dissolves.</p>
            </div>
            <div class="problem-card fade-up">
                <div class="problem-num">03</div>
                <h3>Manual triage wastes staff time</h3>
                <p>Existing client calls and new case inquiries require different responses. Without automated logic, every missed call gets handled the same way.</p>
            </div>
        </div>
    </div>
</section>

<!-- HOW IT WORKS -->
<section id="how">
    <div class="container">
        <div class="section-header fade-up">
            <span class="label">The System</span>
            <h2>How the routing logic works</h2>
            <p>Three stages, built custom to your firm's workflow. Click any step to see the implementation detail.</p>
        </div>
        <div class="flow-steps">
            <div class="step-card fade-up" onclick="toggleStep(this)">
                <div class="step-idx">// step_01</div>
                <div class="step-icon">&#x260E;</div>
                <h3>Missed Call Detected</h3>
                <p>The system monitors your firm's inbound line. When a call goes unanswered, it triggers the routing pipeline in real time.</p>
                <div class="step-detail">
                    <code>event: call_missed<br>trigger: no_answer || voicemail<br>action: initiate_pipeline()<br>response_window: &lt; 30 seconds</code>
                </div>
            </div>
            <div class="step-card fade-up" onclick="toggleStep(this)">
                <div class="step-idx">// step_02</div>
                <div class="step-icon">&#x1F4AC;</div>
                <h3>Professional Text Sent</h3>
                <p>A professional SMS is sent immediately from your office number, configured to your firm's language and tone.</p>
                <div class="step-detail">
                    <code>from: your_office_number<br>template: firm_branded_message<br>caller_id: preserved<br>channel: SMS via API layer</code>
                </div>
            </div>
            <div class="step-card fade-up" onclick="toggleStep(this)">
                <div class="step-idx">// step_03</div>
                <div class="step-icon">&#x21C4;</div>
                <h3>Logic Triage and Routing</h3>
                <p>Automated logic distinguishes new inquiries from existing clients and routes each to your intake form, calendar, or staff queue.</p>
                <div class="step-detail">
                    <code>if caller == new_inquiry:<br>&nbsp;&nbsp;route -&gt; intake_form || calendar<br>elif caller == existing_client:<br>&nbsp;&nbsp;route -&gt; case_update_flow<br>log: all_interactions</code>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- DEMO -->
<section id="demo">
    <div class="container">
        <div class="section-header fade-up">
            <span class="label">Live Demo</span>
            <h2>See the backend architecture</h2>
            <p>A two-minute walkthrough of how the routing logic is structured and deployed.</p>
        </div>
        <div class="demo-wrap fade-up">
            <div class="demo-placeholder">
                <p>[ Loom video embed goes here ]</p>
            </div>
        </div>
    </div>
</section>

<!-- PRICING -->
<section id="pricing">
    <div class="container">
        <div class="section-header fade-up">
            <span class="label">Pricing</span>
            <h2>Straightforward, no surprises</h2>
            <p>One-time build fee plus a monthly retainer. No contracts, no per-seat licensing, no hidden charges.</p>
        </div>
        <div class="pricing-card fade-up">
            <div class="pricing-head">
                <h3>Law Firm Intake Automation Package</h3>
                <p>Custom-built to your firm's workflow. Not a template. Not a plugin.</p>
            </div>
            <div class="pricing-body">
                <div class="pricing-row">
                    <div>
                        <div class="p-label">Architecture Fee</div>
                        <div class="p-desc">Custom routing logic built and deployed for your firm</div>
                    </div>
                    <div class="p-amount">$800<small>one-time</small></div>
                </div>
                <div class="pricing-row">
                    <div>
                        <div class="p-label">Monthly Retainer</div>
                        <div class="p-desc">Server uptime, API connections, and ongoing optimization</div>
                    </div>
                    <div class="p-amount">$175<small>per month</small></div>
                </div>
                <div class="pricing-includes">
                    <span class="label">What is included</span>
                    <div class="include-item">Custom missed-call response logic built to your firm's workflow</div>
                    <div class="include-item">Professional SMS from your existing office number</div>
                    <div class="include-item">New inquiry versus existing client triage</div>
                    <div class="include-item">Routing to your intake form or scheduling calendar</div>
                    <div class="include-item">Ongoing server management and API maintenance</div>
                    <div class="include-item">System optimization as your call volume grows</div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ABOUT -->
<section id="about">
    <div class="container">
        <div class="about-grid">
            <div class="about-text fade-up">
                <span class="label">About</span>
                <h2>Built from the ground up. Not assembled from templates.</h2>
                <p>Discrete Logic Systems architects custom automation pipelines for professional services firms. Every system is built using Python and tailored to your operational workflow, not adapted from an off-the-shelf product.</p>
                <p>The intake system for law firms was developed specifically to address the missed-call conversion gap. Every component, from the routing logic to the triage conditions, is written and maintained directly.</p>
                <a href="mailto:federico@discretelogicsystems.com" class="btn-primary" style="margin-top:1.25rem;">Contact Federico</a>
            </div>
            <div class="stat-grid fade-up">
                <div class="stat-box">
                    <span class="stat-num">100%</span>
                    <div class="stat-lbl">Custom Built</div>
                </div>
                <div class="stat-box">
                    <span class="stat-num">24/7</span>
                    <div class="stat-lbl">System Uptime</div>
                </div>
                <div class="stat-box">
                    <span class="stat-num">Python</span>
                    <div class="stat-lbl">Architecture</div>
                </div>
                <div class="stat-box">
                    <span class="stat-num">South FL</span>
                    <div class="stat-lbl">Local Focus</div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- CONTACT -->
<section id="contact">
    <div class="container">
        <span class="label">Get Started</span>
        <h2>Ready to close the gap in your intake process?</h2>
        <p>Send an email to schedule an implementation overview. The demo is two minutes. The build takes days, not weeks.</p>
        <a href="mailto:federico@discretelogicsystems.com" class="btn-primary" style="font-size:0.95rem; padding:1.1rem 2.5rem;">
            federico@discretelogicsystems.com
        </a>
    </div>
</section>

<footer>
    <p>&copy; 2026 DISCRETE LOGIC SYSTEMS. ALL RIGHTS RESERVED.</p>
</footer>

<script>
/* ============================================================
   1. THREE.JS NETWORK GRAPH (Hero Background)
   ============================================================ */
(function() {
    const canvas  = document.getElementById('hero-canvas');
    const W       = window.innerWidth;
    const H       = window.innerHeight;

    const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
    renderer.setSize(W, H);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

    const scene  = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(55, W / H, 0.1, 1000);
    camera.position.z = 20;

    const group = new THREE.Group();
    scene.add(group);

    /* Nodes */
    const NODE_COUNT  = 60;
    const nodeGeo     = new THREE.SphereGeometry(0.11, 8, 8);
    const nodeData    = [];

    for (let i = 0; i < NODE_COUNT; i++) {
        const theta = Math.random() * Math.PI * 2;
        const phi   = Math.acos(2 * Math.random() - 1);
        const r     = 4.5 + Math.random() * 7;

        const x = r * Math.sin(phi) * Math.cos(theta);
        const y = r * Math.sin(phi) * Math.sin(theta);
        const z = r * Math.cos(phi);

        const bright = 0.35 + Math.random() * 0.45;
        const col    = new THREE.Color().setHSL(0.54, 1.0, bright);
        const mat    = new THREE.MeshBasicMaterial({ color: col });
        const mesh   = new THREE.Mesh(nodeGeo, mat);
        mesh.position.set(x, y, z);
        group.add(mesh);
        nodeData.push({ mesh, phase: Math.random() * Math.PI * 2 });
    }

    /* Edges */
    const EDGE_DIST = 5.8;
    const edgeMat   = new THREE.LineBasicMaterial({
        color: 0x00c8ff, transparent: true, opacity: 0.1
    });

    for (let i = 0; i < nodeData.length; i++) {
        for (let j = i + 1; j < nodeData.length; j++) {
            const a    = nodeData[i].mesh.position;
            const b    = nodeData[j].mesh.position;
            const dist = a.distanceTo(b);
            if (dist < EDGE_DIST) {
                const geo  = new THREE.BufferGeometry().setFromPoints([a.clone(), b.clone()]);
                const line = new THREE.Line(geo, edgeMat.clone());
                line.material.opacity = (1 - dist / EDGE_DIST) * 0.15;
                group.add(line);
            }
        }
    }

    /* Mouse parallax */
    let mx = 0, my = 0;
    document.addEventListener('mousemove', e => {
        mx = (e.clientX / window.innerWidth  - 0.5) * 2;
        my = (e.clientY / window.innerHeight - 0.5) * 2;
    });

    /* Resize */
    window.addEventListener('resize', () => {
        const nw = window.innerWidth, nh = window.innerHeight;
        camera.aspect = nw / nh;
        camera.updateProjectionMatrix();
        renderer.setSize(nw, nh);
    });

    /* Animate */
    const clock = new THREE.Clock();
    (function tick() {
        requestAnimationFrame(tick);
        const t = clock.getElapsedTime();

        group.rotation.y = t * 0.035 + mx * 0.12;
        group.rotation.x = my * 0.07;

        nodeData.forEach(n => {
            const s = 1 + 0.28 * Math.sin(t * 1.6 + n.phase);
            n.mesh.scale.setScalar(s);
        });

        renderer.render(scene, camera);
    })();
})();

/* ============================================================
   2. 3D CARD TILT (How It Works Steps)
   ============================================================ */
document.querySelectorAll('.step-card').forEach(card => {
    card.addEventListener('mousemove', e => {
        const r  = card.getBoundingClientRect();
        const x  = (e.clientX - r.left)  / r.width  - 0.5;
        const y  = (e.clientY - r.top)   / r.height - 0.5;
        card.style.transition = 'border-color 0.3s, box-shadow 0.3s';
        card.style.transform  =
            `perspective(900px) rotateY(${x * 14}deg) rotateX(${-y * 14}deg) translateZ(10px)`;
    });
    card.addEventListener('mouseleave', () => {
        card.style.transition = 'transform 0.55s ease, border-color 0.3s, box-shadow 0.3s';
        card.style.transform  = 'perspective(900px) rotateY(0deg) rotateX(0deg) translateZ(0)';
    });
});

/* ============================================================
   3. STEP DETAIL TOGGLE
   ============================================================ */
function toggleStep(card) {
    const detail = card.querySelector('.step-detail');
    const isOpen = detail.classList.contains('open');
    document.querySelectorAll('.step-detail').forEach(d => d.classList.remove('open'));
    if (!isOpen) detail.classList.add('open');
}

/* ============================================================
   4. SCROLL FADE-UP
   ============================================================ */
const io = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.12 });

document.querySelectorAll('.fade-up').forEach(el => io.observe(el));
</script>

</body>
</html>
